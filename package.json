{
  "name": "streammanager",
  "version": "1.0.0",
  "description": "Gestión de clientes de streaming",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "vercel": {
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist" }
      }
    ]
  }
}
